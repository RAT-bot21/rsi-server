# Copy this to .env and fill in your values
PORT=3000
WEBHOOK_SECRET=replace_this_with_a_long_random_secret

# Generate a secret by running:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
